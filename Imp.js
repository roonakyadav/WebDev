// After creating a function, we must call it to execute its code.
function greet() {
    console.log("Hello, World!");
}

greet(); // Calling the function to execute its code

//===========================================================================================================

// Async with inbuilt module 'fs' (File System)
const fs = require('fs');

// Sync code is ordered by default.
// Async code is ordered only through callbacks (or promises).

//===========================================================================================================

// => is just a shorter syntax for function.
// It does not change async behavior or execution order.

